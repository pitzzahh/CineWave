<UserControl x:Class="CineWave.Components.MovieInfoCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Margin="5">
            <Grid>
                <!-- Movie card content -->
                <StackPanel Margin="10">
                    <TextBlock Text="{Binding MovieName}" FontWeight="Bold" FontSize="16"/>
                    <TextBlock Text="{Binding MoviePrice, StringFormat='Price: {0:C}'}"/>
                    <TextBlock Text="{Binding ReleaseDate, StringFormat='Released on: {0:d}'}"/>
                    <TextBlock Text="{Binding NowShowing, StringFormat='Now Showing: {0}'}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>